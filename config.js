const Data = {
	logChannels: {
		/**
		 * Object that contains configuration for the logging channels and their corresponding guilds.
		 * To bind a particular guild to a specific channel, enter the guild id in "" followed by a :,
		 * then enter the ID of the channel where deleted images should be sent.
		 * Both IDs must be in quotes, otherwise an error will be thrown.
		 * @example "8934298076025698": "84572569875460987"
		 */
		"a_guild_id": "a_channel_id",
	},
};

export { Data };